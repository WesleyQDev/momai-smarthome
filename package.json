{
  "name": "momai-smart-home",
  "version": "1.0.21",
  "description": "MomAI Smart Home - Extensão de automação residencial com suporte a Home Assistant e provedores plugáveis",
  "main": "src/index.ts",
  "scripts": {
    "build": "node build.mjs",
    "test": "node test-runner.ts",
    "check": "pnpm build && pnpm test"
  },
  "keywords": [
    "momai",
    "extension",
    "smarthome",
    "homeassistant",
    "iot",
    "automation"
  ],
  "author": "MomAI Team",
  "license": "MIT",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2",
    "dotenv": "^16.4.5"
  }
}
